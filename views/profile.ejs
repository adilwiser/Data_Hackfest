<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Profile</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Press+Start+2P&display=swap" rel="stylesheet">
    <style>
        :root {
            --background-color: #0a0a0a;
            --text-color: #00ff00;
            --primary-color: #ff00ff;
            --secondary-color: #ffff00;
            --border-color: #00ff00;
        }
        body {
            font-family: 'Press Start 2P', cursive;
            margin: 0;
            background-color: var(--background-color);
            color: var(--text-color);
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            text-align: center;
        }
        .container {
            width: 90%;
            max-width: 800px;
        }
        header {
            padding: 20px;
            margin-bottom: 40px;
            border-bottom: 4px solid var(--border-color);
        }
        header h1 {
            margin: 0;
            font-size: 2em;
            color: var(--secondary-color);
            text-shadow: 4px 4px 0px var(--primary-color);
        }
        .button {
            background-color: var(--primary-color);
            color: var(--background-color);
            border: 4px solid var(--border-color);
            padding: 15px 25px;
            font-size: 1em;
            cursor: pointer;
            text-decoration: none;
            display: inline-block;
            text-transform: uppercase;
        }
        #profile-container {
            background-color: #1a1a1a;
            border: 4px solid var(--border-color);
            padding: 30px;
        }
        #profile-avatar {
            width: 100px;
            height: 100px;
            border: 4px solid var(--border-color);
            margin-bottom: 20px;
        }
        #profile-name {
            font-size: 1.5em;
            color: var(--secondary-color);
            margin: 0;
        }
        #profile-email {
            font-size: 0.8em;
            color: var(--text-color);
            margin-top: 10px;
            margin-bottom: 30px;
            word-break: break-all;
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <h1>Your Profile</h1>
        </header>
        <main>
            <div id="profile-container">
                <% if (typeof updated !== 'undefined' && updated) { %>
                  <div style="color: var(--secondary-color); margin-bottom: 15px; font-size: 1em;">Profile updated successfully!</div>
                <% } %>
                <img id="profile-avatar" src="<%= user.picture %>" alt="User Avatar" onerror="this.onerror=null;this.src='/default-avatar.png';">
                <h2 id="profile-name"><%= user.nickname %></h2>
                <p id="profile-email"><%= user.email %></p>
                <a href="/profile/edit" class="button">Edit Profile</a>
                <a href="/logout" class="button" style="margin-left: 10px;">Logout</a>
            </div>
        </main>
    </div>
</body>
</html>